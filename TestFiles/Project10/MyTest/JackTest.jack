class foo {
    static int b;
    field int x, y;
    static int z;
    field char a;


    method int bank(int x1, int y1, int z1) {
        var int xyz, xyz1;
        var char abc;

        let xyz = bank2.foo(x1, y1, z1) + 3;
    }

    method int bank2(int x2) {
        var int xyz;

        let xyz = x2;
    }
}